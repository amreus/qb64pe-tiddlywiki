This is ready for testing.

If you would like to test it, you need a tiddlywiki file in the same folder as the server. `curl` can be used to fetch an empty tiddlywiki:

```
curl https://tiddlywiki.com/empty.html > empty.html
```

Do open an issue if you would like to contribute. Thanks.

